Testing the Ansible Role with Vagrant and Ubuntu 16.04

Requirements:
 - Virtualbox
 - Vagrant

Simple Testing:
```bash
vagrant up
```

Rerun the Test:
```bash
vagrant provision
```

Checking the Test Environment:
```bash
vagrant ssh
```
